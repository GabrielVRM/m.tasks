version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
  pg:
    image: bitnami/postgresql/latest
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: ttasks
    ports:
      - "5432:5432"
